{
    "name": "EC2 Performance Lab",

    "image": "mcr.microsoft.com/devcontainers/base:noble",

    "features": {
        "ghcr.io/devcontainers/features/python:1": {
            "version": "3.12"
        }
    },

    "postCreateCommand": "sudo apt update && sudo apt install -y time linux-tools-common linux-tools-generic && sudo sysctl -w kernel.perf_event_paranoid=-1 && sudo ln -sf /usr/lib/linux-tools-6.8.0-139/perf /usr/local/bin/perf"
}
